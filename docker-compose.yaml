version: '3'

services:
  news_db:
    container_name: news_db
    image: h0d0user/news_db:v0.0.41
    restart: always
    network_mode: host
#    command: sleep 9999
    volumes:
      - "shared-volume:/test.db"
      - ".secrets.toml:/opt/.secrets.toml"

  truth_seeker:
    container_name: truth_seeker
    image: h0d0user/truth_seeker:v0.0.41
    restart: always
    network_mode: host
#    command: sleep 9999
    depends_on:
      - "news_db"
    volumes:
      - "shared-volume:/test.db"
      - ".secrets.toml:/opt/.secrets.toml"

  datapath:
    container_name: datapath
    image: h0d0user/datapath:v0.0.41
    restart: always
    network_mode: host
#    command: sleep 9999
    depends_on:
      - "news_db"
    volumes:
      - "shared-volume:/test.db"
      - ".secrets.toml:/opt/.secrets.toml"

volumes:
  shared-volume:
